# Design Rules

## Border Radius
- Do NOT keep any corners rounded.
- Everything in the user interface MUST have strict zero border radius (`border-radius: 0px`).
- Applies to all buttons, cards, modals, dropdowns, inputs, badges, avatars, and containers.
